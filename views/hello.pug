extends layout.pug

block content
    if name
        h2 Welcome #{name}!
    else
        form(actions='/hello', method='post')
            label Enter your name here: 
                input(type= 'text', name= 'username')
            button(type='submit') Submit