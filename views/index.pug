extends layout.pug

block content
        section#content
            h2 Welcome #{name}!
        form(action="/goodbye",method="post")
            button(type="submit") Good Bye
        
        a(href= '/cards') Begin!